---
# Role metadata for hardware_interfaces

galaxy_info:
  author: Scramble Robot Team
  description: Configure hardware interfaces (I2C, SPI) and udev rules for robotics hardware
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions:
        # ansible-lint's bundled Galaxy schema does not yet include noble.
        # The kit playbook still enforces Ubuntu 24.04 before applying this role.
        - all

dependencies: []
