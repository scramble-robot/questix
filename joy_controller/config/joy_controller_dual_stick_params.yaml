# Node options; operator controls live in questix_control_config/config/controls.*.yaml
# under joy_controller_dual_stick. Keep this wildcard for custom node names.
/**:
  ros__parameters:
    joy_topic: "/joy"
    debug_mode: false
