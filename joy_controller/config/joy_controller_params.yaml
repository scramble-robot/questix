# Hardware-independent node options. Operator controls live in
# questix_control_config/config/controls.*.yaml (joy_controller section).
joy_controller:
  ros__parameters:
    use_sim_time: false
    debug_mode: false
